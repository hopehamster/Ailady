# Codex Workflow (Aria) — thin pointer

**Follow the single canon: [`ops/aria/protocols/execution-loop.md`](../ops/aria/protocols/execution-loop.md).**
This file adds only Codex-specific notes; it does NOT restate the loop (no parallel truth — `ops/aria/README.md:70`). The canon contains the full loop, the web verification gates, and the if-blocked guidance.

## Codex-specific adaptations
- **Catch up** via `.codex/CATCHUP.md` + `.codex/ACTIVE_SLICE.md` (generated by `scripts/sync-agent-adapters.ps1` from `ops/aria/current/*`).
- **Checkpoint** via `scripts/checkpoint-work.ps1` (scoped `-OnlyPaths`; auto-runs adapter sync).
- **If blocked:** read `ops/aria/current/EXECUTION_CHECKLIST.md`, update the GitHub issue with the blocker + evidence.
